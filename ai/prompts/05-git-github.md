Before GitHub:
1. Check git status.
2. Review changed files.
3. Confirm no secrets, API keys, passwords or .env files are committed.
4. Confirm the project runs.
5. Summarize changes.

Then provide the exact Git commands for a clean commit and GitHub push.

Do not create a remote repository or push unless explicitly authorized.
