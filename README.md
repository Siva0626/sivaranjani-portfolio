# Sivaranjani Portfolio — Codex Project

AI project identity: PortfolioDev

This is a project knowledge base for AI-assisted/vibe coding. It is not model training. The AI should read the relevant project files as context.

Workflow:
Requirements → UX/UI → Architecture → Frontend → Backend if needed → Database if needed → API if needed → Testing → Git → GitHub → Render deployment.

Start with a responsive frontend. Add Supabase/backend/API only when a real requirement needs them.

Render = deployment/hosting.
Cloudflare = later DNS/CDN/security layer, not the frontend application host.
