# Database

Supabase is optional.

Do not create tables until a real requirement needs persistent/dynamic data.

When database work starts, keep schema documentation, migrations, seed data and security/RLS notes here.

Never store passwords or API secrets in database documentation.
